# Core PDF extraction libraries (install at least one)
pdfplumber>=0.9.0       # Preferred: better layout and table handling
PyPDF2>=3.0.0           # Fallback: lightweight, pure-Python

# Optional: OCR support for scanned/image-based PDFs
# pdf2image>=1.16.0     # Converts PDF pages to images
# pytesseract>=0.3.10   # OCR engine (also requires Tesseract system install)
# Pillow>=9.0.0         # Required by pdf2image
